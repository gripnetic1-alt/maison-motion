import Link from "next/link";
import { Brand } from "@/components/brand";
import { ArrowRight, CheckIcon } from "@/components/icons";

const plans = [{ name: "Starter", price: "19", detail: "Pour lancer ses premiers actifs", features: ["3 films / mois", "Storyboard par catégorie", "Aperçu filigrané"], featured: false }, { name: "Pro", price: "49", detail: "Pour les agents qui avancent", features: ["12 films / mois", "Formats réseaux sociaux", "Export HD sans filigrane"], featured: true }, { name: "Agency", price: "129", detail: "Pour les équipes qui accélèrent", features: ["Films illimités", "5 catégories premium", "Support prioritaire"], featured: false }];

export default function PricingPage() { return <main><header className="site-header"><Link href="/"><Brand /></Link><Link className="text-link" href="/dashboard">Retour au dashboard</Link></header><section className="story-section" style={{ paddingTop: 80 }}><div className="story-heading"><div><p className="eyebrow">Le bon rythme pour vous</p><h1 className="serif" style={{ margin: "16px 0 0", fontSize: "clamp(46px, 6vw, 78px)", lineHeight: .94 }}>Choisissez votre<br /><span style={{ color: "var(--clay)", fontStyle: "italic" }}>cadence.</span></h1></div><p>Vidéo cinématique pour actifs premium à partir de vos photos : chaque plan commence par la même intention, quelle que soit la signature présentée.</p></div><div className="property-grid" style={{ marginTop: 70, alignItems: "stretch" }}>{plans.map((plan) => <article key={plan.name} className="new-card" style={{ position: "relative", background: plan.featured ? "var(--ink)" : "rgba(255,255,255,.28)", color: plan.featured ? "var(--paper)" : "var(--ink)", borderColor: plan.featured ? "var(--ink)" : "var(--line)" }}>{plan.featured && <span className="property-badge" style={{ left: 25, top: 25 }}>Le plus choisi</span>}<p className="eyebrow" style={{ color: plan.featured ? "var(--lime)" : "var(--muted)", marginTop: plan.featured ? 29 : 0 }}>{plan.name}</p><h2 className="serif" style={{ margin: "15px 0 8px", fontSize: 38 }}>{plan.price} €<span style={{ fontFamily: "Trebuchet MS", fontSize: 12, letterSpacing: 0, color: plan.featured ? "#aab4aa" : "var(--muted)" }}> / mois</span></h2><p style={{ color: plan.featured ? "#aab4aa" : "var(--muted)", fontSize: 12 }}>{plan.detail}</p><div className="form-divider" style={{ background: plan.featured ? "rgba(244,240,231,.18)" : "var(--line)" }} />{plan.features.map((feature) => <p key={feature} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 12, color: plan.featured ? "var(--paper)" : "var(--ink-soft)" }}><CheckIcon size={14} /> {feature}</p>)}<Link className={`button ${plan.featured ? "lime" : "secondary"}`} style={{ width: "100%", marginTop: 18 }} href={`/auth?plan=${plan.name.toLowerCase()}`}>Choisir {plan.name} <ArrowRight size={14} /></Link></article>)}</div></section><footer className="site-footer"><Brand /><span>Simple, transparent, sans engagement.</span></footer></main>; }
